admin.mandatry_addr_args:
	   admin command address [options]
	you can also approve command by using the following syntax
	   approve PASSWORD command address [options]

admin.log:
	_ARG0_ shows the last _ARG1_ line of the logfile
	as the file is too big.
	_ARG0_ all shows all of logfile.
	_ARG0_ -N shows the last N lines.
	For example: _ARG0_ -200 shows the last 200 lines.

admin.reconfigurable_error:
	admin (approve) command modifies or removes files defined only in
	@REMOTE_RECONFIGURABLE_FILES
