admin.mandatry_addr_args:
	   admin コマンド アドレス オプション
	形式で使って下さい。	

admin.log:
	_ARG0_ はログファイルの最後の _ARG1_ 行を表示しています
	(ログファイルが大きいためです)。
	_ARG0_ all はログファイル全部を
	_ARG0_ -N はログファイルの最後のN行を表示します。
	例: _ARG0_ -200 は最後の200行を表示します。

admin.reconfigurable_error:
	adminコマンドを使ってリモートでファイル操作(e.g. 上書き)可能なファイルは
	@REMOTE_RECONFIGURABLE_FILESという配列で定義されているものに限られています。
