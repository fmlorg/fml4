requried: (differs from os to os)

	libpng
	gd
	zlib
	http-analyze 2.3's source

configure:

	# mkdir /var/www/local
	# mkdir /var/www/local/lists	# for referer analyze
	# env OPT=-d sh wwwstat.sh	# daily mode
	# env OPT=-m sh wwwstat.sh	# monthly mode

  [wwwstat.sh]

	#!/bin/sh

	DIR=/var/www/local

	cd $DIR || exit 1

	exec /usr/local/bin/http-analyze -v -v $OPT \
	   -f /etc/http-analyze.conf \
	        /usr/local/apache/logs/access_log $*

	echo cannot reach here
	exit 1;

  [apache/conf/httpd.conf]

	# statistics
	Alias /local/ "/var/www/local/"

	<Directory "/var/www/local">
	    Options Multiviews
	    AllowOverride None
  
	    Order deny,allow
	    deny from all
	    allow from IP.add.re.ss
	</Directory>

$Id$
