auth.ok:
	認証されました。

auth.fail:
	認証に失敗しました。

auth.change_password.ok:
	パスワード変更しました。

auth.change_password.fail:
	パスワード更新に失敗しました。

auth.invalid_password:
	パスワードが間違っています。

auth.password_unchanged:
	パスワード変更されませんでした。

auth.password.cannot_open:
	パスワード・ファイルを開くことができません。

auth.password.cannot_mk_pwdb:
	パスワード・ファイルを生成するができませんでした。

auth.password.cannot_mk_pwdb.bak:
	パスワード・ファイルのバックアップ生成に失敗しました。

auth.password.rename.fail:
	パスワード・ファイルのバックアップ生成(rename)に失敗しました。

auth.please_use_pgp:
	警告: _ARG0_: どうしてPGPモードで無駄なパスワード認証をするのでしょう？

auth.admin_not_member:
	<_ARG0_>はリモートでfmlをメインテナンスできる管理者
	として登録されていません。最初に
	   コマンド    admin addadmin _ARG0_
	   (shell上で) makefml addadmin _ARG0_
	などを実行して登録する必要があります。
	詳しくはインストール・ファイル(doc/INSTALL.jp)やdoc/op.jpなどを
	見て下さい。

auth.should_be_from_member:
	エラー: <_ARG0_>はメンバーではありません。
	メンバーに登録されていない人は使用できません。
	From: をチェックしてください。
