#!/usr/bin/perl

# Copyright (C) 1994 Noboru Ikuta <ikuta@crc.co.jp>
#
# rbody: MIME base64 decoder Ver.0.01
#        MIME base64 encoding されたデータをデコードする。
#
# 使い方 : [ [j]perl ] rbody [ ファイル名 ...]
#
#         mimer.pl は、環境変数 HOME→rbodyと同じ→通常の@INC の順にサ
#         ーチされます。
#
# 配布条件 : 著作権は放棄しませんが、配布・改変は自由とします。

$home = $ENV{'HOME'};
$perl_dir = $0;
$perl_dir =~ s/\\/\//g;
substr($perl_dir, rindex($perl_dir, "/")) = '';
unshift(@INC, $home, $perl_dir);
require 'mimer.pl';

binmode STDOUT;
while(<>){
    print &bodydecode($_);
}
print &bdeflush;
